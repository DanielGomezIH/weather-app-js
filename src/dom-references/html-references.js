const searchBoxElement = document.querySelector('.search input');
const searchBtnElement = document.querySelector('.search button');

const errorParagraphElement = document.querySelector('.error');

const weatherInformationElement = document.querySelector('.weather');

const temperatureHeadingElement = document.querySelector('.temp');

const citySubheadingElement = document.querySelector('.city');

const humidityParagraphElement = document.querySelector('.humidity');

const windSpeedParagraphElement = document.querySelector('.wind');

const weatherIconElement = document.querySelector('.weather-icon');

export {
  searchBoxElement,
  searchBtnElement,
  errorParagraphElement,
  weatherInformationElement,
  temperatureHeadingElement,
  citySubheadingElement,
  humidityParagraphElement,
  windSpeedParagraphElement,
  weatherIconElement,
};
